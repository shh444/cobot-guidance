# 툴 센터 포인트 (TCP)

:::{admonition} 이 장에서 배우는 것
:class: note
- TCP가 무엇인지
- 왜 툴 교체 후 TCP를 다시 봐야 하는지
- TCP teaching을 어떻게 검증하면 좋은지
:::

## TCP를 초보자용으로 설명하면

TCP(Tool Center Point)는 **로봇이 "공구 끝이 여기 있다"고 생각하는 기준점**입니다.

즉, 로봇 팔 끝 금속 플랜지가 중요한 것이 아니라, 실제 작업에서 의미 있는 지점이 중요합니다.

예를 들면:

- 그리퍼 손끝
- 흡착패드 중심
- 드라이버 비트 끝
- 디스펜서 노즐 끝

```{figure} ../_static/tcp-concept-diagram.svg
:alt: TCP 개념도
:width: 100%

TCP는 로봇 플랜지가 아니라 실제 공구 끝 작업점을 기준으로 설정합니다.
```

## 왜 TCP가 중요한가

아래 문제가 보이면 TCP를 먼저 의심하세요.

- MoveL이 이상하게 보인다
- 공구가 작업물에 비스듬히 접근한다
- 같은 점을 찍어도 삽입 자세가 어긋난다
- Feature를 잘 잡았는데도 방향이 어색하다

TCP가 맞지 않으면 "로봇이 틀린 게 아니라, 로봇이 믿는 공구 끝 위치가 틀린 것" 입니다.

## Tool Flange와 TCP의 차이

| 항목 | 의미 |
| --- | --- |
| Tool Flange | 로봇 팔 끝의 기계적 체결 기준 |
| TCP | 실제 작업 기준점 |

툴이 길어지거나 꺾여 있으면 flange와 TCP는 크게 달라질 수 있습니다.

## 초보자용 TCP 설정 흐름

### 1. 툴의 실제 작업점을 먼저 정한다
예: 흡착패드 중앙, 핀 끝, 노즐 끝

### 2. TCP 이름을 먼저 정한다
예: `tcp_gripper_tip`, `tcp_vacuum_center`, `tcp_driver_bit`

### 3. TCP를 측정/teaching 한다
가능하면 고정된 한 점을 기준으로 여러 자세에서 접근합니다.

### 4. MoveL로 검증한다
작업점 근처에서 짧게 직선 진입/이탈을 해 보며 방향과 거리 감각을 확인합니다.

## TCP teaching 실무 팁

### 서로 다른 자세에서 측정하기
한 방향에서만 비슷하게 잡으면 계산 품질이 떨어집니다. 서로 충분히 다른 각도에서 접근하는 것이 좋습니다.

### 세 점보다 네 번째 검증점을 추가하기
계산은 최소 점 수로 가능하더라도, 검증용 한 점을 더 쓰면 오류를 빨리 잡을 수 있습니다.

### 툴 교체 후에는 이름만 복사하지 않기
툴 형상이 바뀌면 기존 TCP 이름을 그대로 재사용하지 않는 편이 좋습니다.

:::{tip}
사내 문서에는 TCP 표를 따로 두세요. `TCP 이름 / 용도 / 기준점 사진 / 최근 검증일 / 관련 프로그램` 형태가 좋습니다.
:::

## 검증용 짧은 실습

### 실습 1. 수직 진입

- 작업점 위 30 mm 접근점 생성
- MoveL로 아래로 진입
- MoveL로 다시 위로 복귀

기대 결과: 공구가 옆으로 휘지 않고 의도한 방향으로 직선 이동

### 실습 2. 표면 따라가기

- 평평한 면을 기준으로 두 점 생성
- 같은 높이에서 MoveL 수행

기대 결과: 공구가 면을 따라 일정한 자세로 이동

## 여러 TCP를 쓸 때의 기본 전략

툴 하나에 실제 작업점이 여러 개일 수도 있습니다. 예를 들면 듀얼 그리퍼나 길이가 다른 팁입니다.

이때 초보자에게는 다음 전략을 권장합니다.

- TCP를 목적별로 명확히 나눈다
- 프로그램 상단에 현재 어떤 TCP를 쓰는지 주석/설명 추가
- 먼저 하나의 TCP로 안정화한 뒤 여러 TCP로 확장

## 자주 하는 실수

### TCP 위치는 맞는데 방향을 신경 안 씀
삽입, 스크루, 비전 정렬 작업은 방향이 특히 중요합니다.

### TCP를 잡고도 Feature는 기본값 그대로 둠
TCP와 Feature는 같이 생각해야 합니다. 공구 기준과 작업물 기준이 둘 다 맞아야 합니다.

### 툴 교체 후 Payload만 바꾸고 TCP는 안 바꿈
질량과 기준점은 다른 문제입니다. 둘 다 확인해야 합니다.

## 문서에 넣으면 좋은 자료

- TCP 기준점이 보이는 툴 사진
- flange에서 TCP까지 거리 스케치
- TCP teaching 화면 캡처
- 검증 경로 예시 GIF 또는 스크린샷

## 체크리스트

- [ ] TCP를 flange와 구분해서 설명할 수 있다
- [ ] 현재 툴의 실제 작업점이 어디인지 말할 수 있다
- [ ] TCP 검증을 MoveL 진입/이탈로 해 볼 수 있다
- [ ] 툴 변경 시 TCP 재확인이 필요하다는 점을 이해했다

## 다음에 읽을 장

다음 장에서는 **좌표계와 Feature** 를 다룹니다. TCP가 공구 기준이라면, Feature는 작업물 기준입니다.
